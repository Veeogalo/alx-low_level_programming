#include "main.h"
/**
* main - Entry point
* _abs - computes absolute value of an integer
* @i: the absolute value
* Return: absolute value
*/
int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (i * -1);
}
}
